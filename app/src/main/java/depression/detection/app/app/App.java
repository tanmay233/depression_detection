/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package depression.detection.app.app;

import depression.detection.app.list.LinkedList;

import static depression.detection.app.utilities.StringUtils.join;
import static depression.detection.app.utilities.StringUtils.split;
import static depression.detection.app.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
